name: Feature request
description: Suggest a capability or a change in behaviour
labels: ["enhancement"]
body:
  - type: markdown
    attributes:
      value: >-
        Worth skimming
        [what's in scope](https://github.com/krcm0209/sous/blob/main/CONTRIBUTING.md#whats-in-scope)
        first — it saves writing up something the project has already decided
        against.

  - type: textarea
    id: problem
    attributes:
      label: What problem are you hitting?
      description: >-
        The situation that prompted this, rather than the solution. What were
        you trying to get sous to do?
    validations:
      required: true

  - type: textarea
    id: proposal
    attributes:
      label: What should sous do instead?
    validations:
      required: true

  - type: textarea
    id: alternatives
    attributes:
      label: Alternatives you considered (optional)
      description: >-
        Including anything you tried that nearly worked — a config change, a
        smaller context window, a different model.

  - type: checkboxes
    id: scope
    attributes:
      label: Scope
      options:
        - label: >-
            I understand sous deliberately targets Apple silicon, and that
            running the daemon elsewhere is not a current goal
          required: true
        - label: >-
            This does not have sous execute a tool, hold a credential or add a
            permission layer of its own — what the local model may do is Claude
            Code's to govern — or if it does, I have said so explicitly above
          required: true
